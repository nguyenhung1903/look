# Benchmark snapshot (2026-04-15)

Command (run once):

```bash
cargo run --manifest-path core/engine/Cargo.toml --example perf_bench
```

Environment:

- db path: `/Users/kunkka07xx/Library/Application Support/look/look.db`
- candidate count: `1887`
- profile: `dev` (unoptimized + debuginfo)

Indexing throughput:

- elapsed: `46 ms`
- throughput: `40705.46 candidates/s`

Comparison baseline:

- source: `docs/bench-notes/2026-04-08.md` (Two-stage retrieval update section)
- baseline candidate count: `3043`

Important: dataset sizes differ (`1887` vs `3043`), so percentages below are directional, not strict apples-to-apples.

## Query latency comparison (p50)

| query | baseline p50 (ms) | current p50 (ms) | result |
| --- | ---: | ---: | --- |
| empty (`""`) | 0.675 | 0.402 | **40.4% faster** |
| `sa` | 51.035 | 4.296 | **91.6% faster** |
| `net` | 51.264 | 4.963 | **90.3% faster** |
| `doc` | 50.750 | 4.652 | **90.8% faster** |
| `visual` | 50.484 | 5.895 | **88.3% faster** |
| `privacy security` | 51.596 | 11.256 | **78.2% faster** |
| `a"safari` | 2.367 | 0.813 | **65.7% faster** |
| `f"note` | 33.482 | 3.356 | **90.0% faster** |
| `d"down` | 15.234 | 1.487 | **90.2% faster** |
| `r"^visual.*` | 2.465 | 1.708 | **30.7% faster** |

## Query latency comparison (p95)

| query | baseline p95 (ms) | current p95 (ms) | result |
| --- | ---: | ---: | --- |
| empty (`""`) | 0.747 | 0.447 | **40.2% faster** |
| `sa` | 52.273 | 4.454 | **91.5% faster** |
| `net` | 53.255 | 5.137 | **90.4% faster** |
| `doc` | 51.824 | 4.792 | **90.8% faster** |
| `visual` | 51.018 | 6.026 | **88.2% faster** |
| `privacy security` | 52.547 | 11.422 | **78.3% faster** |
| `a"safari` | 2.536 | 0.868 | **65.8% faster** |
| `f"note` | 34.420 | 3.469 | **89.9% faster** |
| `d"down` | 15.609 | 1.559 | **90.0% faster** |
| `r"^visual.*` | 2.655 | 1.886 | **29.0% faster** |

## Fuzzy micro-bench comparison (avg/op)

The benchmark now reports two modes:

- `raw`: calls `fuzzy_score(query, title)` (includes query prepare work)
- `prepared_query`: precomputes `prepare_query(query)` then calls `fuzzy_score_prepared`

| mode | scenario | baseline avg/op (ns) | current avg/op (ns) | result |
| --- | --- | ---: | ---: | --- |
| `raw` | `launcher_like_short_queries` | 300 | 977 | **225.7% slower** |
| `raw` | `gap_and_boundary_patterns` | 307 | 982 | **219.9% slower** |
| `prepared_query` | `launcher_like_short_queries` | 300 | 869 | **189.7% slower** |
| `prepared_query` | `gap_and_boundary_patterns` | 307 | 873 | **184.4% slower** |

Prepared-query improvement vs raw (same run):

- `launcher_like_short_queries`: **11.1% faster**
- `gap_and_boundary_patterns`: **11.1% faster**

Notes:

- Current search latencies are much lower on this run, but candidate count is also much lower.
- Fuzzy per-op cost remains meaningfully higher than the 2026-04-08 baseline in both raw and prepared modes.
- The new prepared-query mode helps isolate query preparation overhead and gives clearer profiling direction.
